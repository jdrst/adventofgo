# adventofcode

## Linting:

`golangci-lint run`

## Test:

### within day
`go test`
### all of them (in project root)
`go test ./...`

### Benchmark:

`go test -bench=B -benchmem`

## Run:

`go run .`